server:
  port: 8088
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/diqksrk/config-repo
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

management:
  endpoints:
    web:
      exposure:
        include: "bus-refresh"
